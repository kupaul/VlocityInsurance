import { LightningElement } from 'lwc';

import { BaseState } from "vlocity_ins/baseState";
import cardActive from "./npFooter.html";

export default class npFooter extends BaseState(LightningElement) {
  
}